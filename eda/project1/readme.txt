The script that generates all the images is:

basic_plots.r

After including these file with the source command, the function main will generate the images:

main()

Note: Please note that the function checkFile in the file fileFunctions.r will download
the dataset if this it is not present in the folder. This script was run in a windows pc.
